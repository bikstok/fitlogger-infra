apiVersion: v1
kind: Service
metadata:
  name: fitlogger-app
  namespace: fitlogger
  labels:
    app.kubernetes.io/name: fitlogger-app
    app.kubernetes.io/part-of: fitlogger
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: fitlogger-app
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
